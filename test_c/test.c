#include <stdio.h>

int main(int argc, char *argv[])
{
    // printf("%s\n", argv[0]);
    // for (int i = 1; i < argc; i++)
    // {
    //     printf("%s\n", argv[i]);
    // }
    int i, j;
    int n = scanf("%d%d", &i, &j);
    printf("%d %d\n", i, j);
    return 0;
}

