#include <stdio.h>

void see();

int main(int argc, char *argv[])
{
    // printf("%s\n", argv[0]);
    // for (int i = 1; i < argc; i++)
    // {
    //     printf("%s\n", argv[i]);
    // }
    int i;
    void see();

    printf("%d\n", i);
    // int p = see();

    return 0;
}

void see() {
    for (int i = 0; i < 10; i++)
    {
        printf("aaa\n");
    }
    
}
